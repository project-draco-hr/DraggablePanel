{
  VideoViewModel item=getContent();
  frameTitle.setText(context.getResources().getString(item.getTitle()));
  Picasso.with(context).load(item.getImage()).placeholder(R.color.soft_white).into(frameImage);
}
